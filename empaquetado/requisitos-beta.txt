# Lo que instala la beta en su runtime (capa A). Es el perfil que prueba
# `tests/test_paquete_ligero.py::DENTRO_DEL_PAQUETE`: si se añade algo aquí sin
# que ese test lo pida, el instalador engorda sin motivo.
#
# Mismas versiones que `requirements.txt`. Las transitivas las fija
# `construir.py` con `-c requirements.lock.txt`.
ezdxf==1.4.4
shapely==2.1.2
flask==3.1.3
reportlab==5.0.0
pyyaml==6.0.3
jsonschema==4.26.0
waitress==3.0.2
python-dotenv==1.2.3
