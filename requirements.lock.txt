# ArchMuse -- entorno completo y reproducible (REFACTOR_MASTERPLAN tarea 12).
#
# Generado con `pip freeze` sobre el venv en el que la suite pasa en verde.
# Incluye TODAS las distribuciones instaladas, transitivas incluidas: es lo
# que hace que una instalacion dentro de seis meses sea la misma que la de
# hoy. `requirements.txt` dice de que depende ArchMuse y por que; este archivo
# dice exactamente que se instala.
#
#     pip install -r requirements.lock.txt
#
# Las transitivas importan mas de lo que parece: `werkzeug` (bajo flask),
# `httpx` (bajo anthropic) y `numpy` (bajo shapely y trimesh) han roto
# aplicaciones en cambios de version mayor sin que la dependencia directa
# cambiara de numero. Fijar solo las directas no reproduce nada.
#
# Incluye `pytest`, que no es dependencia del producto: el venv del que sale
# este archivo es el mismo en el que se ejecuta la suite, y un lock que no
# reproduce el entorno de medicion no reproduce la medicion.
#
# Como regenerarlo: subir la version en `requirements.txt`, instalar, pasar
# `pytest` ENTERO, y solo entonces `pip freeze > requirements.lock.txt`.
#
# Referencia: Python 3.14 en Windows. `ifcopenshell` y `mapbox_earcut` traen
# binarios precompilados por plataforma; en otro sistema operativo pip
# resolvera la rueda que corresponda con estas mismas versiones.

annotated-types==0.8.0
anthropic==0.120.2
anyio==4.14.2
attrs==26.1.0
blinker==1.9.0
certifi==2026.7.22
cffi==2.1.1
charset-normalizer==3.4.9
click==8.4.2
colorama==0.4.6
cryptography==50.0.0
distro==1.9.0
docstring_parser==0.18.0
ezdxf==1.4.4
Flask==3.1.3
fonttools==4.63.0
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
idna==3.18
ifcopenshell==0.8.5
iniconfig==2.3.0
isodate==0.7.2
itsdangerous==2.2.0
Jinja2==3.1.6
jiter==0.16.0
jsonschema==4.26.0
jsonschema-specifications==2025.9.1
lark==1.3.1
mapbox_earcut==2.0.0
MarkupSafe==3.0.3
numpy==2.5.1
packaging==26.3
pdfminer.six==20260107
pillow==12.3.0
pluggy==1.6.0
pycparser==3.0
pydantic==2.13.4
pydantic_core==2.46.4
Pygments==2.20.0
pyparsing==3.3.2
pypdf==6.15.0
pypdfium2==5.12.1
pytest==9.1.1
python-dateutil==2.9.0.post0
python-dotenv==1.2.3
PyYAML==6.0.3
referencing==0.37.0
reportlab==5.0.0
rpds-py==2026.6.3
shapely==2.1.2
six==1.17.0
sniffio==1.3.1
trimesh==5.0.0
typing-inspection==0.4.2
typing_extensions==4.16.0
waitress==3.0.2
Werkzeug==3.1.8
